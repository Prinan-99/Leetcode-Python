class Solution:
    def finalValueAfterOperations(self, operations: List[str]) -> int:
        final=0
        for operation in operations:
            if operation[1]=="+":
                final+=1;
            else:
                final-=1
        return final
